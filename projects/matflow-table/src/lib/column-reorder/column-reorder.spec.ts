import { ColumnReorder } from './column-reorder';

describe('ColumnReorder', () => {
  it('should create an instance', () => {
    const directive = new ColumnReorder();
    expect(directive).toBeTruthy();
  });
});
