import { ColumnReorderList } from './column-reorder-list';

describe('ColumnReorderList', () => {
  it('should create an instance', () => {
    const directive = new ColumnReorderList();
    expect(directive).toBeTruthy();
  });
});
