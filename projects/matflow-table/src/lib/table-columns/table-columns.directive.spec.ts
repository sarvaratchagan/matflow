import { TableColumnsDirective } from './table-columns.directive';

describe('TableColumnsDirective', () => {
  it('should create an instance', () => {
    const directive = new TableColumnsDirective();
    expect(directive).toBeTruthy();
  });
});
