import { InjectionToken } from '@angular/core';
import { TableSettingsSource } from './table-settings-source';

export const TABLE_SETTINGS_SOURCE = new InjectionToken<TableSettingsSource>(
  'TABLE_SETTINGS_SOURCE'
);
