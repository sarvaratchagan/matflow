import { MatflowTable } from './matflow-table';

describe('MatflowTable', () => {
  it('should create an instance', () => {
    const directive = new MatflowTable();
    expect(directive).toBeTruthy();
  });
});
